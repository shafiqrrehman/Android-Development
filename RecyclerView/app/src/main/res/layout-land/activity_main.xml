<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recyclerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="3dp"
        android:scrollbars="vertical"
        android:background="@android:color/darker_gray"
        android:layout_marginBottom="100dp"/>

    <EditText
        android:id="@+id/insertEditText"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginStart="15dp"
        android:layout_marginBottom="25dp"
        android:inputType="number"/>

    <Button
        android:id="@+id/insertButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="insert"
        android:layout_alignBottom="@+id/insertEditText"
        android:layout_toEndOf="@+id/insertEditText"/>

    <EditText
        android:id="@+id/removeEditText"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="70dp"
        android:layout_alignBottom="@+id/insertButton"
        android:layout_toEndOf="@+id/insertButton"
        android:inputType="number"/>

    <Button
        android:id="@+id/removeButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="remove"
        android:layout_alignBottom="@+id/removeEditText"
        android:layout_toEndOf="@+id/removeEditText"/>

</RelativeLayout>